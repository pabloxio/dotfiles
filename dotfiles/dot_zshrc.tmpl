# vim: ft=zsh

export ZSH="${HOME}/.oh-my-zsh"

plugins=(
  asdf
  direnv
  fzf-tab
  git
  starship
  vi-mode
  zsh-autosuggestions
)

source ${ZSH}/oh-my-zsh.sh

# Loading ${HOME}.zsh.d/* config files
for ZSH_CONFIG_FILE in ${HOME}/.zsh.d/*; do
    source "${ZSH_CONFIG_FILE}"
done
